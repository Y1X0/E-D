---
import NotFound from '~/views/NotFound.astro';
import { PREFIXED_LOCALES, type Locale } from '~/i18n';

export const getStaticPaths = () => PREFIXED_LOCALES.map((locale) => ({ params: { locale } }));

const locale = Astro.params.locale as Locale;
---
<NotFound locale={locale} />
