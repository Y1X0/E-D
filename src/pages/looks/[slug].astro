---
import Look from '~/views/Look.astro';
import { looks } from '~/data/looks';

export const getStaticPaths = () => looks.map((look) => ({ params: { slug: look.slug }, props: { look } }));

const { look } = Astro.props;
---
<Look locale="en" look={look} />
