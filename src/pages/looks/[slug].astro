---
import Look from '~/views/Look.astro';
import { getRoutes } from '~/content';

export async function getStaticPaths() {
  const { looks } = await getRoutes();
  return looks.map((l) => ({ params: { slug: l.slug } }));
}
---
<Look locale="en" slug={Astro.params.slug!} />
