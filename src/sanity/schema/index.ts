import { localeString, localeText, photo } from './localized';
import { collection, look, siteSettings } from './documents';

export const schemaTypes = [localeString, localeText, photo, collection, look, siteSettings];
